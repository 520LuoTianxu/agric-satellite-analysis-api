# ─────────────────────────────────────────────────────────────────────────────
# agric-satellite-analysis - Development Docker Compose
#
# Usage:
#   docker compose -f docker-compose.yml -f docker-compose.dev.yml up --build
# Default object store: Aliyun OSS.
#
# Exposes service ports to localhost for local development and debugging.
# Not used in production - see docker-compose.prod.yml instead.
# ─────────────────────────────────────────────────────────────────────────────

services:
  db:
    ports:
      - "5432:5432"

  redis:
    ports:
      - "6379:6379"

  api:
    ports:
      - "8000:8000"

  web:
    ports:
      - "3000:3000"
